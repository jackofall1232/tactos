# integration-test CI workflow stub — filled in by the build loop, not by l00prite
name: Integration
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Integration CI not yet configured — filled in by the build loop"
