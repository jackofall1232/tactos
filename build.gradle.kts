// Root build file: intentionally empty of plugin applications so pure-JVM
// modules can configure (and be tested) in environments without access to
// Google's Maven repository. Android plugins are applied per-module.
